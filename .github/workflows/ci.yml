name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "24"
  PNPM_VERSION: "10.15.0"

jobs:
  # ── API: lint ──────────────────────────────────────────────────────────────
  api-lint:
    name: API — lint & typecheck
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dev dependencies
        run: pip install -e ".[dev]"

      - name: Ruff lint
        run: ruff check app/ tests/ scripts/ migrations/

      - name: Ruff format check
        run: ruff format --check app/ tests/ scripts/ migrations/

  # ── API: tests ─────────────────────────────────────────────────────────────
  api-test:
    name: API — tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api

    services:
      # pgvector/pgvector:pg16 has pgvector pre-installed (AMD64 compatible).
      # PostGIS is installed into the running container via docker exec below.
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: dcos
          POSTGRES_PASSWORD: dcos
          POSTGRES_DB: dcos_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U dcos -d dcos_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install PostGIS into running Postgres container
        run: |
          # Find the pgvector container (it's the only Postgres running)
          PG_CONTAINER=$(docker ps --filter "ancestor=pgvector/pgvector:pg16" -q)
          echo "Postgres container: $PG_CONTAINER"

          # Install PostGIS packages inside the container
          docker exec $PG_CONTAINER bash -c "
            apt-get update -qq &&
            apt-get install -y --no-install-recommends \
              postgresql-16-postgis-3 \
              postgresql-16-postgis-3-scripts &&
            rm -rf /var/lib/apt/lists/*
          "

          # Create extensions and dcos_app role (matches infra/postgres/init.sql)
          sudo apt-get install -y --no-install-recommends postgresql-client
          PGPASSWORD=dcos psql -h localhost -U dcos -d dcos_test -c "
            CREATE EXTENSION IF NOT EXISTS postgis;
            CREATE EXTENSION IF NOT EXISTS vector;
            CREATE EXTENSION IF NOT EXISTS pg_trgm;
            CREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";
            CREATE EXTENSION IF NOT EXISTS btree_gin;
            DO \$\$ BEGIN
              IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname = 'dcos_app') THEN
                CREATE ROLE dcos_app WITH LOGIN PASSWORD 'dcos_app'
                  NOSUPERUSER NOINHERIT NOCREATEDB NOCREATEROLE NOBYPASSRLS;
              END IF;
            END \$\$;
            GRANT CONNECT ON DATABASE dcos_test TO dcos_app;
            GRANT USAGE ON SCHEMA public TO dcos_app;
          "
          echo "Extensions and roles ready"

      - name: Install Python dependencies
        run: pip install -e ".[dev]"

      - name: Run Alembic migrations
        env:
          DATABASE_URL: postgresql+asyncpg://dcos:dcos@localhost:5432/dcos_test
        run: alembic upgrade head

      - name: Grant table permissions to dcos_app + seed reference data
        run: |
          # Grant dcos_app SELECT/INSERT/UPDATE/DELETE on all tables created by migrations
          PGPASSWORD=dcos psql -h localhost -U dcos -d dcos_test -c "
            GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO dcos_app;
            GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO dcos_app;
          "
          # Seed reference data (departments, districts, wards) needed by integration tests
          DATABASE_URL="postgresql://dcos:dcos@localhost:5432/dcos_test" \
            python -m scripts.seed

      - name: Run tests
        env:
          DATABASE_URL: postgresql+asyncpg://dcos:dcos@localhost:5432/dcos_test
          REDIS_URL: redis://localhost:6379
          ENVIRONMENT: local
        run: pytest --cov=app --cov-report=xml -q

      - name: Upload coverage
        uses: codecov/codecov-action@v5
        with:
          files: apps/api/coverage.xml
          flags: api
        continue-on-error: true

  # ── Web: lint + typecheck + build ──────────────────────────────────────────
  web-lint:
    name: Web — lint & typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: ESLint
        run: pnpm --filter web lint

      - name: TypeScript
        run: |
          pnpm --filter web typecheck
          pnpm --filter @dcos/ui typecheck
          pnpm --filter @dcos/types typecheck

  web-build:
    name: Web — build
    runs-on: ubuntu-latest
    needs: web-lint
    env:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm --filter web build

  # ── Docker image ─────────────────────────────────────────────────────────────
  docker-build:
    name: Docker — build API image
    runs-on: ubuntu-latest
    needs: [api-lint, api-test]
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Build API image
        uses: docker/build-push-action@v6
        with:
          context: apps/api
          push: false
          tags: dcos-api:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
